{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "(Debug) Build",
            "type": "shell",
            "command": "odin build modules/runner/neat.odin -file --debug --opt=0 --vet --out=bin/neat",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}